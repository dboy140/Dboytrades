__pycache__/
*.pyc
.venv/
venv/
.env

# Raw and derived corpora are large and re-fetchable; the knowledge base
# (kb/) is the durable, reviewed artifact and IS tracked.
data/raw/*
data/derived/*
!data/raw/.gitkeep
!data/derived/.gitkeep

system/build/
.pytest_cache/
